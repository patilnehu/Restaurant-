/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "themewagon.github.io",
      "images.pexels.com",
      "images.unsplash.com",
    ], // Add all allowed domains
  },
};

module.exports = nextConfig;
